## Grade Calculator (Python)
A simple CLI tool to calculate academic averages.

**Key features:**
* Uses a `for` loop to iterate through a list of subjects.
* Dynamically handles scores and provides feedback based on the final average.
* Clean and scalable code structure.
